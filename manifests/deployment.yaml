apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-log-output-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-log-output
  template:
    metadata:
      labels:
        app: web-log-output
    spec:
      containers:
        - name: web-log-output
          image: isalxmkh/web-log-output-app:v1.01